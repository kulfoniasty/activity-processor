export default function activityProcessor() {
  const process = () => {};

  const actions = {
    loadSummary: () => {
      return actions;
    },
    loadLaps: () => {
      return actions;
    },
    loadSamples: () => {
      return actions;
    },
    process,
  };

  return actions;
}
